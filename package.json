{
  "name": "kitchencloud",
  "version": "1.0.0",
  "description": "KitchenCloud - A MERN stack recipe sharing application",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": "18.x"
  }
}